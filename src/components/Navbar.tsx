"use client";
import { FC } from "react";

interface NavbarProps {}

const Navbar: FC<NavbarProps> = ({}) => {
  return (
    <nav className="navbar">
      <div className="width">nav</div>
    </nav>
  );
};

export default Navbar;
